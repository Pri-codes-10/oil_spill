# requirements.txt
# OWNER: shared — B2 is gatekeeper (integration duty, WORKFLOW.md §4.5).
# RULE: one dependency per line, alphabetical, pinned. This is what lets git
# auto-merge cleanly instead of conflicting when three people add deps on the
# same day. Append only — do not reorder existing lines.

affine==2.4.0
asyncpg==0.29.0
cdsapi==0.7.7
copernicusmarine==2.4.1
fastapi==0.115.0
httpx==0.27.2          # test-only: needed by fastapi.testclient.TestClient
numpy==2.2.6             # bumped from 1.26.4: copernicusmarine/cdsapi need numpy>=2.1; capped <2.3 since scipy==1.13.1 warns it needs numpy<2.3
pandas==2.2.2
psycopg2-binary==2.9.9
pyproj==3.6.1
pytest==8.2.2
python-dotenv==1.0.1
rasterio==1.3.10
scikit-image==0.24.0
scipy==1.13.1
shapely==2.1.2           # bumped from 2.0.5: 2.0.5's union_all() breaks under numpy>=2.1 (TypeError in create_collection)
uvicorn==0.30.6
websockets==12.0
xarray==2026.7.0        # bumped from 2024.7.0: copernicusmarine==2.4.1 requires xarray>=2024.10.0

# --- optional, day-5 upgrade path (B2 §7 / §10) ---
# opendrift==1.11.9
